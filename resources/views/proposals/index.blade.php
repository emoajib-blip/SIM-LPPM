<x-layouts.app :title="__('Proposal')">
    <div class="card card-stacked">
        <div class="card-body">
            <div class="d-flex align-items-center justify-content-between mb-3">
                <div>
                    <h2 class="mb-1 text-primary">{{ __('Proposal') }}</h2>
                    <p class="mb-0 text-muted">{{ __('Kelola pengajuan proposal penelitian dan pengabdian di sini.') }}</p>
                </div>
            </div>
            <p class="mb-0 text-secondary">{{ __('Belum ada data yang ditampilkan. Fitur ini akan segera tersedia.') }}</p>
        </div>
    </div>
</x-layouts.app>
